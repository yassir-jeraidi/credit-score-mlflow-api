name: CI - Build, Test & Push

on:
  # CI runs on main, dev, and feature branches
  push:
    branches: [main, dev, feature/*]
    tags:
      - "v*"
  # CI runs on PRs targeting dev and main
  pull_request:
    branches: [dev, main]
  workflow_dispatch:
    inputs:
      push_images:
        description: "Push images to registry"
        required: true
        default: true
        type: boolean

env:
  PYTHON_VERSION: "3.11"
  NODE_VERSION: "20"
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ===========================================================================
  # Python Code Quality (Linting)
  # ===========================================================================
  python-lint:
    name: Python Linting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff black isort flake8
      - name: Run linting
        run: |
          flake8 app ml tests
          black --check app ml tests
          isort --check-only app ml tests

  # ===========================================================================
  # TypeScript/UI Code Quality (Linting)
  # ===========================================================================
  typescript-lint:
    name: TypeScript Linting
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ui
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: ui/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: Run linting
        run: npm run lint

  # ===========================================================================
  # Python Unit Tests (with MLflow mocking)
  # ===========================================================================
  python-unit-tests:
    name: Python Unit Tests
    runs-on: ubuntu-latest
    needs: python-lint
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run unit tests
        run: pytest tests/test_model.py
        env:
          PYTHONPATH: .

  # ===========================================================================
  # Integration Tests (Docker Compose Stack)
  # ===========================================================================
  integration-tests:
    name: Integration Tests (Docker)
    runs-on: ubuntu-latest
    needs: [python-lint, python-unit-tests]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Start services
        run: |
          docker compose -f docker/docker-compose.yml up -d --build postgres mlflow api
          sleep 20  # Wait for services to be ready
      - name: Run Integration Tests
        run: |
          # Use docker exec or run pytest from host against the running services
          # Assuming host can access mapped ports
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          POSTGRES_HOST=localhost POSTGRES_PORT=5432 pytest tests/test_api.py
        env:
          PYTHONPATH: .

  # ===========================================================================
  # Build and Push API Docker Image
  # ===========================================================================
  build-api:
    name: Build & Push API Image
    runs-on: ubuntu-latest
    needs: [python-lint, typescript-lint]
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}
      short-sha: ${{ steps.vars.outputs.short-sha }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set variables
        id: vars
        run: echo "short-sha=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for API image
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch,suffix=-api
            type=ref,event=pr,suffix=-api
            type=semver,pattern={{version}},suffix=-api
            type=semver,pattern={{major}}.{{minor}},suffix=-api
            type=sha,prefix=sha-,suffix=-api
            type=raw,value=api,enable={{is_default_branch}}

      - name: Build and push API image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_images != 'false') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        if: github.event_name != 'pull_request'
        continue-on-error: true
        with:
          image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}-api
          artifact-name: sbom-api.spdx.json

  # ===========================================================================
  # Build and Push UI Docker Image
  # ===========================================================================
  build-ui:
    name: Build & Push UI Image
    runs-on: ubuntu-latest
    needs: [python-lint, typescript-lint]
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
      image-digest: ${{ steps.build.outputs.digest }}
      short-sha: ${{ steps.vars.outputs.short-sha }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set variables
        id: vars
        run: echo "short-sha=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for UI image
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch,suffix=-ui
            type=ref,event=pr,suffix=-ui
            type=semver,pattern={{version}},suffix=-ui
            type=semver,pattern={{major}}.{{minor}},suffix=-ui
            type=sha,prefix=sha-,suffix=-ui
            type=raw,value=ui,enable={{is_default_branch}}

      - name: Build and push UI image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: ./ui
          file: ./ui/Dockerfile
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_images != 'false') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64
          build-args: |
            API_BASE_URL=http://api:8000
            NEXT_PUBLIC_API_URL=http://localhost:8000

  # ===========================================================================
  # Build and Push MLflow Docker Image
  # ===========================================================================
  build-mlflow:
    name: Build & Push MLflow Image
    runs-on: ubuntu-latest
    needs: [python-lint]
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for MLflow image
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch,suffix=-mlflow
            type=ref,event=pr,suffix=-mlflow
            type=semver,pattern={{version}},suffix=-mlflow
            type=semver,pattern={{major}}.{{minor}},suffix=-mlflow
            type=sha,prefix=sha-,suffix=-mlflow
            type=raw,value=mlflow,enable={{is_default_branch}}

      - name: Build and push MLflow image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./mlflow.Dockerfile
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_images != 'false') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64

  # ===========================================================================
  # Notify on completion
  # ===========================================================================
  notify:
    name: Build Status Summary
    runs-on: ubuntu-latest
    needs: [build-api, build-ui, build-mlflow]
    if: always()
    steps:
      - name: Check build status
        run: |
          if [ "${{ needs.build-api.result }}" == "success" ] && [ "${{ needs.build-ui.result }}" == "success" ] && [ "${{ needs.build-mlflow.result }}" == "success" ]; then
            echo "✅ All builds succeeded!"
            echo ""
            echo "📦 Images pushed to GHCR:"
            echo "  API:    ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}-api"
            echo "  UI:     ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}-ui"
            echo "  MLflow: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}-mlflow"
            echo ""
            echo "🔖 Short SHA: ${{ needs.build-api.outputs.short-sha }}"
          else
            echo "❌ Some builds failed"
            echo "  API:    ${{ needs.build-api.result }}"
            echo "  UI:     ${{ needs.build-ui.result }}"
            echo "  MLflow: ${{ needs.build-mlflow.result }}"
            exit 1
          fi
